import { CheckCircle, Users, FileImage } from 'lucide-react';

interface Creator {
  id: string;
  name: string;
  avatar: string;
  cover: string;
  subscribers: number;
  postsCount: number;
  price: number;
  verified: boolean;
  description: string;
}

interface CreatorCardProps {
  creator: Creator;
  onViewProfile: (creatorId: string) => void;
}

export function CreatorCard({ creator, onViewProfile }: CreatorCardProps) {
  return (
    <div 
      className="bg-gray-900 rounded-xl overflow-hidden border border-gray-800 hover:border-pink-500 transition cursor-pointer group"
      onClick={() => onViewProfile(creator.id)}
    >
      <div className="relative h-40 overflow-hidden">
        <img 
          src={creator.cover} 
          alt={creator.name}
          className="w-full h-full object-cover group-hover:scale-105 transition duration-300"
        />
        <div className="absolute inset-0 bg-gradient-to-t from-gray-900 to-transparent" />
      </div>

      <div className="relative px-4 pb-4">
        <div className="flex items-end gap-3 -mt-8 mb-3">
          <img 
            src={creator.avatar} 
            alt={creator.name}
            className="w-16 h-16 rounded-full border-4 border-gray-900 object-cover"
          />
          <div className="flex-1 mb-1">
            <div className="flex items-center gap-1">
              <h3 className="text-lg font-semibold text-white">{creator.name}</h3>
              {creator.verified && (
                <CheckCircle className="w-4 h-4 text-blue-500 fill-blue-500" />
              )}
            </div>
          </div>
        </div>

        <p className="text-gray-400 text-sm mb-3 line-clamp-2">{creator.description}</p>

        <div className="flex items-center gap-4 text-sm text-gray-500 mb-3">
          <div className="flex items-center gap-1">
            <Users className="w-4 h-4" />
            <span>{creator.subscribers.toLocaleString('pt-BR')}</span>
          </div>
          <div className="flex items-center gap-1">
            <FileImage className="w-4 h-4" />
            <span>{creator.postsCount} posts</span>
          </div>
        </div>

        <div className="flex items-center justify-between">
          <div className="text-white">
            <span className="text-2xl font-bold">R$ {creator.price.toFixed(2)}</span>
            <span className="text-gray-500 text-sm">/mês</span>
          </div>
          <button className="px-4 py-2 bg-gradient-to-r from-pink-500 to-purple-600 text-white rounded-lg font-semibold hover:opacity-90 transition">
            Ver Perfil
          </button>
        </div>
      </div>
    </div>
  );
}