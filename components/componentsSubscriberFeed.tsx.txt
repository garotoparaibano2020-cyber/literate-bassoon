import { Heart, MessageCircle, Share2, Lock, Video, CheckCircle } from 'lucide-react';

interface SubscriberFeedProps {
  subscribedCreatorIds: string[];
}

const mockPosts = [
  {
    id: '1',
    creatorId: '1',
    creatorName: 'Luna Star',
    creatorAvatar: 'https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=400&h=400&fit=crop',
    verified: true,
    type: 'photo',
    content: 'https://images.unsplash.com/photo-1509631179647-0177331693ae?w=800&h=1000&fit=crop',
    caption: 'Novo set de fotos! Espero que gostem 💕',
    likes: 1234,
    comments: 89,
    timestamp: '2 horas atrás',
    isLive: false
  },
  {
    id: '2',
    creatorId: '1',
    creatorName: 'Luna Star',
    creatorAvatar: 'https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=400&h=400&fit=crop',
    verified: true,
    type: 'live',
    content: 'https://images.unsplash.com/photo-1485178575877-1a13bf489dfe?w=800&h=600&fit=crop',
    caption: '🔴 AO VIVO - Vem conversar comigo!',
    viewers: 234,
    timestamp: 'Ao vivo agora',
    isLive: true
  },
  {
    id: '3',
    creatorId: '1',
    creatorName: 'Luna Star',
    creatorAvatar: 'https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=400&h=400&fit=crop',
    verified: true,
    type: 'video',
    content: 'https://images.unsplash.com/photo-1487412720507-e7ab37603c6f?w=800&h=1000&fit=crop',
    caption: 'Vídeo exclusivo para vocês ✨',
    likes: 2156,
    comments: 167,
    timestamp: '1 dia atrás',
    duration: '12:34',
    isLive: false
  }
];

export function SubscriberFeed({ subscribedCreatorIds }: SubscriberFeedProps) {
  const posts = mockPosts.filter(post => subscribedCreatorIds.includes(post.creatorId));

  if (posts.length === 0) {
    return (
      <div className="max-w-3xl mx-auto px-4 py-12 text-center">
        <Lock className="w-16 h-16 text-gray-600 mx-auto mb-4" />
        <h2 className="text-2xl font-bold text-white mb-2">Nenhum conteúdo ainda</h2>
        <p className="text-gray-400">Assine criadores para ver o conteúdo exclusivo deles aqui</p>
      </div>
    );
  }

  return (
    <div className="max-w-3xl mx-auto px-4 py-8">
      <h1 className="text-3xl font-bold text-white mb-8">Seu Feed</h1>

      <div className="space-y-6">
        {posts.map((post) => (
          <div key={post.id} className="bg-gray-900 rounded-xl overflow-hidden border border-gray-800">
            <div className="p-4 flex items-center gap-3">
              <img 
                src={post.creatorAvatar} 
                alt={post.creatorName}
                className="w-10 h-10 rounded-full object-cover"
              />
              <div className="flex-1">
                <div className="flex items-center gap-1">
                  <span className="font-semibold text-white">{post.creatorName}</span>
                  {post.verified && (
                    <CheckCircle className="w-4 h-4 text-blue-500 fill-blue-500" />
                  )}
                </div>
                <span className="text-sm text-gray-500">{post.timestamp}</span>
              </div>
              {post.isLive && (
                <div className="px-3 py-1 bg-red-600 text-white text-sm font-semibold rounded-full flex items-center gap-1">
                  <span className="w-2 h-2 bg-white rounded-full animate-pulse" />
                  AO VIVO
                </div>
              )}
            </div>

            <div className="relative">
              <img 
                src={post.content} 
                alt="Post content"
                className="w-full aspect-[4/5] object-cover"
              />
              {post.type === 'video' && !post.isLive && (
                <div className="absolute inset-0 flex items-center justify-center">
                  <div className="w-16 h-16 bg-black/50 backdrop-blur-sm rounded-full flex items-center justify-center">
                    <Video className="w-8 h-8 text-white" />
                  </div>
                  <div className="absolute bottom-4 right-4 px-2 py-1 bg-black/70 text-white text-sm rounded">
                    {post.duration}
                  </div>
                </div>
              )}
              {post.isLive && (
                <div className="absolute top-4 right-4 px-3 py-1 bg-black/70 text-white text-sm rounded-full flex items-center gap-2">
                  <Users className="w-4 h-4" />
                  {post.viewers} assistindo
                </div>
              )}
            </div>

            <div className="p-4">
              <div className="flex items-center gap-6 mb-3">
                <button className="flex items-center gap-2 text-gray-400 hover:text-pink-500 transition">
                  <Heart className="w-6 h-6" />
                  <span>{post.likes?.toLocaleString('pt-BR')}</span>
                </button>
                <button className="flex items-center gap-2 text-gray-400 hover:text-blue-500 transition">
                  <MessageCircle className="w-6 h-6" />
                  <span>{post.comments}</span>
                </button>
                <button className="flex items-center gap-2 text-gray-400 hover:text-green-500 transition">
                  <Share2 className="w-6 h-6" />
                </button>
              </div>
              <p className="text-white">
                <span className="font-semibold mr-2">{post.creatorName}</span>
                {post.caption}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}

function Users({ className }: { className?: string }) {
  return (
    <svg 
      className={className}
      xmlns="http://www.w3.org/2000/svg" 
      viewBox="0 0 24 24" 
      fill="none" 
      stroke="currentColor" 
      strokeWidth="2" 
      strokeLinecap="round" 
      strokeLinejoin="round"
    >
      <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2" />
      <circle cx="9" cy="7" r="4" />
      <path d="M22 21v-2a4 4 0 0 0-3-3.87" />
      <path d="M16 3.13a4 4 0 0 1 0 7.75" />
    </svg>
  );
}